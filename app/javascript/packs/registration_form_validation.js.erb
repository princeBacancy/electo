$(document).ready(function() {
alert("outer")
  $('#user_first_name').change(function() {
  alert("inner)
    if ($('#user_user_name').is(':empty')){
      alert("if")
      console.log("dd")
      $('#user_name_error').val("user_name can't be blank")
    } 
  });
});